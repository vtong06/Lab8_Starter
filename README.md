# Lab8-Starter
1. Graceful degradation means an app still works in a simpler way when an advanced feature is not available. Service workers help improve the app, but the app doesn't depend on them. The basic recipe app still functions without offline caching.
